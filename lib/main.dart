import 'package:flutter/material.dart';

void main() {
  runApp(MaterialApp(
    home: Scaffold(
      appBar: AppBar(title: Text('Work Hours Tracker')),
      body: Center(child: Text('Версия 2.0 — чистый старт')),
    ),
  ));
}
